{{title model.build.buildTitle}}
{{build-container
  build=model.build
  activeSnapshotId=activeSnapshotId
  updateActiveSnapshotId=(route-action "updateActiveSnapshotId")
  openSnapshotFullModal='openSnapshotFullModal'
  isShowingModal=model.build.isShowingModal
  snapshots=model.snapshots
  showSupport=(route-action "showSupport")
}}
{{outlet}}
